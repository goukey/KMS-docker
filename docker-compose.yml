version: '3'

services:
  bitwarden:
    container_name: bitwarden
    image: bitwardenrs/server:latest
    restart: always
    volumes:
      - ./bitwarden/data:/data
      - /etc/certs:/ssl
    env_file:
      - config.env
    ports:
      - '444:80'
